{
  "name": "test-render",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && mv dist/src/index.html index.html && mkdir -p assets && mv dist/assets/* assets/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "~5.6.2",
    "vite": "^6.0.1"
  },
  "dependencies": {
    "nanoid": "^5.0.9",
    "rxjs": "^7.8.1"
  }
}
